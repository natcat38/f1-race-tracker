# --- Go ---
/gateway
/gateway.exe
/bench/loadtest          # built load-test binary (the .exe form is caught by *.exe below)
*.exe
*.test
*.out

# --- Python ---
__pycache__/
*.py[cod]
.venv/
venv/
.env
# FastF1 downloads cache (large; never commit)
cache/
.fastf1/

# --- Node / React ---
node_modules/
web/dist/
!web/dist/.gitkeep
web/.vite/
# Baked by `go run ./cmd/bake-static` for the GitHub Pages static demo — CI
# generates this fresh on every deploy (.github/workflows/pages.yml), it's
# never committed.
web/public/static-demo/

# --- OS / IDE ---
.DS_Store
Thumbs.db
.idea/

# Note: data/replays/*.jsonl ARE committed (curated demo clips) — do not ignore them.

# local verification artifacts
.playwright-mcp/
m3a-live-lane-silverstone.png
m3b-python-live-lane.png
m4-compare-monza-2023-vs-2024.png

# Host-linked F1TV token for the beta live path (ADR-0007) — never committed.
secrets/

# Claude Code local session state (memory lives in /memory, which IS committed).
.claude/
