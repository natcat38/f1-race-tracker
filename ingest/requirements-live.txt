# Extra deps for the live-SignalR path, on top of requirements.txt.
#
# signalrcore is NOT here: it must be installed with --no-deps, so it lives in
# requirements-live-nodeps.txt (which explains why). Installing it normally drags in
# a vulnerable msgpack; installing the older version that avoids that breaks the
# websocket connection outright. See ADR-0007.
redis==8.1.0
